<div class="banner">
  <%= image_tag "udslogo.png", alt: "Logo", class: "logo" %>
</div>

<div class="about-section">
  <div class="about-content">
    <div class="about-images">
      <%= image_tag asset_path('paysage.jpg'), class: 'about-image top-left', alt: 'Paysage' %>
      <%= image_tag asset_path('meditation.jpg'), class: 'about-image top-right', alt: 'Méditation' %>
      <%= image_tag asset_path('rando_montagne.jpg'), class: 'about-image bottom-left', alt: 'Randonnée' %>
      <%= image_tag asset_path('nature.jpg'), class: 'about-image bottom-right', alt: 'Nature' %>
    </div>

    <h2>L’Univers des sens</h2>
    <h3>RANDONNÉE & YOGA POUR SE RECONNECTER À L’ESSENTIEL</h3>
    <p>Avec Jennifer Camp, accompagnatrice en montagne et professeure de Yoga Vinyasa, explorez deux façons uniques de vous reconnecter à vous-même.
Participez à des randonnées yoga au cœur de la nature, alliant marche consciente et pratiques de pleine présence.
Découvrez des cours de Yoga Vinyasa fluides et dynamiques, pour harmoniser souffle, mouvement et énergie.
Rejoignez L’Univers des Sens et offrez-vous une véritable parenthèse de bien-être et de ressourcement.</p>
  </div>
</div>

<div class="practice-section">
  <div class="practice-container">

    <!-- Titre principal -->
    <h2 class="practice-title">Pratiquons Ensemble</h2>

    <!-- Conteneur des cartes -->
    <div class="practice-content">

      <!-- Carte Yoga -->
      <article class="card">
        <img class="card__background" src="<%= asset_path('yoga.jpg') %>" alt="Yoga">
        <div class="card__content flow">
          <div class="card__content--container flow">
            <h2 class="card__title">Yoga</h2>
            <p class="card__description">Détendez votre esprit et renforcez votre corps avec des cours de yoga adaptés à tous les niveaux. Retrouvez équilibre et sérénité à chaque séance..</p>
          </div>
          <a href="<%= courses_path %>" class="card__button">Explorer</a>
        </div>
      </article>

      <!-- Carte Randonnée Yoga -->
      <article class="card">
        <img class="card__background" src="<%= asset_path('rando_nature.jpg') %>" alt="Randonnée Yoga">
        <div class="card__content flow">
          <div class="card__content--container flow">
            <h2 class="card__title">Randonée Yoga</h2>
            <p class="card__description">Reconnectez-vous à la nature avec les randonnée yoga. Une expérience immersive pour ressourcer corps et esprit..</p>
          </div>
          <a href="<%= hikes_path %>" class="card__button">Explorer</a>
        </div>
      </article>

    </div>
  </div>
</div>


<div class="events-section" id="evenements">
  <h2 class="event-title1">Prochains Événements Yoga & Randonnées</h2>

  <div class="events-container">
    <!-- Affichage du prochain cours de yoga -->
    <% if @next_course %>
      <div class="event-row">
        <!-- Affichage de l’image du cours -->
        <div class="event-image">
          <% if @next_course.photo.attached? %>
            <%= image_tag @next_course.photo, class: "event-photo" %>
          <% else %>
            <%= image_tag "default_course.jpg", class: "event-photo" %>
          <% end %>
        </div>

        <div class="event-details">
          <h3 class="event-title"><%= @next_course.title %></h3>
          <span class="event-info"><strong>Date :</strong>&nbsp;<%= l(@next_course.date, format: "%A %d %B %Y") %></span>
          <span class="event-info"><strong>Tarif : </strong>&nbsp;<%= @next_course.price %> €</span>
          <a href="<%= course_path(@next_course) %>" class="event-button">Plus d'informations →</a>
        </div>
      </div>
    <% else %>
      <p class="no-event">Aucun cours de yoga prévu prochainement.</p>
    <% end %>

    <!-- Affichage de la prochaine randonnée yoga -->
<% if @next_hike %>
  <div class="event-row">
    <!-- Affichage de l’image de la randonnée -->
    <div class="event-image">
      <% if @next_hike.photo.attached? %>
        <%= image_tag @next_hike.photo, class: "event-photo" %>
      <% else %>
        <%= image_tag "default_hike.jpg", class: "event-photo" %>
      <% end %>
    </div>

    <div class="event-details">
      <h3 class="event-title"><%= @next_hike.title %></h3>

      <% if @next_hike.date.present? && @next_hike.time.present? %>
        <% datetime = DateTime.new(
              @next_hike.date.year,
              @next_hike.date.month,
              @next_hike.date.day,
              @next_hike.time.hour,
              @next_hike.time.min
            ) %>
        <span class="event-info"><strong>Départ :</strong>&nbsp;<%= l(datetime, format: :long) %></span>
      <% else %>
        <span class="event-info"><strong>Départ :</strong>&nbsp;<%= l(@next_hike.date, format: "%A %d %B %Y") %></span>
      <% end %>

      <span class="event-info"><strong>Tarif :</strong>&nbsp;<%= @next_hike.price %> €</span>
      <a href="<%= hike_path(@next_hike) %>" class="event-button">Plus d'informations →</a>
    </div>
  </div>
<% else %>
  <p class="no-event">Aucune randonnée prévue prochainement.</p>
<% end %>
  </div>

  <!-- Liens vers toutes les sessions -->
  <div class="event-links">
    <a href="<%= courses_path %>" class="events-link">Voir tous les cours de yoga</a>
    <a href="<%= hikes_path %>" class="events-link">Voir toutes les randonées</a>
  </div>
</div>


<div class="image-section">
  <div class="image-container">
    <%= image_tag asset_path('paysage_2.jpg'), class: 'main-image', alt: 'Grande image principale' %>
    <%= image_tag asset_path('papillon.jpg'), class: 'small-image top-right', alt: 'Petite image en haut à droite' %>
    <%= image_tag asset_path('rando.jpg'), class: 'small-image bottom-left', alt: 'Petite image en bas à gauche' %>
  </div>
</div>

<div class="contact-section" id="contact">
  <div class="contact-container">
    <div class="contact-form">
      <h2>Contact</h2>
      <%= form_with url: contacts_path, method: :post, local: true, html: { class: "contact-form-wrapper" } do |f| %>
        <div class="form-group">
          <%= f.label :first_name, "Prénom" %>
          <%= f.text_field :first_name, required: true, class: "form-control" %>
        </div>
        <div class="form-group">
          <%= f.label :last_name, "Nom" %>
          <%= f.text_field :last_name, required: true, class: "form-control" %>
        </div>
        <div class="form-group">
          <%= f.label :email, "Email" %>
          <%= f.email_field :email, required: true, class: "form-control" %>
        </div>
        <div class="form-group">
          <%= f.label :phone, "Numéro de téléphone" %>
          <%= f.telephone_field :phone, class: "form-control" %>
        </div>
        <div class="form-group">
          <%= f.label :message, "Message" %>
          <%= f.text_area :message, rows: 4, required: true, class: "form-control" %>
        </div>
        <%= f.submit "Envoyer", class: "btn-primary" %>
      <% end %>
    </div>
    <div class="contact-image">
      <%= image_tag asset_path('neige.jpg'), alt: 'Contact' %>
    </div>
  </div>
</div>
