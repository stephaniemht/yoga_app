<nav class="navbar navbar-expand-lg">
  <div class="container">
    <!-- Logo -->
   <%= link_to root_path, data: { turbo: false }, class: "navbar-brand" do %>
    <%= image_tag "udslogo.png", alt: "Logo", class: "navbar-logo" %>
   <% end %>


    <!-- Burger -->
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <!-- Liens -->
    <div class="collapse navbar-collapse justify-content-end" id="navbarNav">
      <ul class="navbar-nav">
      <li class="nav-item"><%= link_to "Accueil", root_path, class: "nav-link", data: { turbo: false } %></li>
      <li class="nav-item"><%= link_to "Yoga", courses_path, class: "nav-link" %></li>
      <li class="nav-item"><%= link_to "Randonnées", hikes_path, class: "nav-link" %></li>
      <li class="nav-item"><%= link_to "Événements", root_path(anchor: 'evenements'), class: "nav-link", data: { turbo: false } %></li>
      <li class="nav-item"><%= link_to "Contact", root_path(anchor: 'contact'), class: "nav-link", data: { turbo: false } %></li>


        <% if user_signed_in? && current_user.admin? %>
          <li class="nav-item dropdown">
            <a class="nav-link dropdown-toggle" href="#" id="adminDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
              Admin
            </a>
            <ul class="dropdown-menu dropdown-menu-end" aria-labelledby="adminDropdown">
              <li><%= link_to "Tableau de bord", admin_dashboard_path, class: "dropdown-item", data: { turbo: false } %></li>
              <li><hr class="dropdown-divider"></li>
              <li>
                <form id="logout-form" action="<%= destroy_user_session_path %>" method="post" style="display: none;">
                  <input type="hidden" name="_method" value="delete">
                  <%= hidden_field_tag :authenticity_token, form_authenticity_token %>
                </form>
                <a href="#" onclick="document.getElementById('logout-form').submit();" class="dropdown-item">Déconnexion</a>
              </li>
            </ul>
          </li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>
